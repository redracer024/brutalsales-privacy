// Very minimal fix for expo modules issues
println "Applying fixes for Expo modules"

// Disable toolchain validation completely
gradle.projectsLoaded {
    rootProject.allprojects {
        tasks.withType(org.gradle.api.tasks.compile.JavaCompile) {
            options.fork = true
        }
    }
}